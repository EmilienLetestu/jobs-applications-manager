doctrine:
    dbal:
        # configure these for your database server
        driver: 'pdo_mysql'
        driver: 'pdo_sqlite'
        server_version: '5.7'
        charset: utf8mb4
        default_table_options: